#include<stdio.h>
int main()
{
    printf("size of int:%d bytes",sizeof(int));
    printf("size of long:%d bytes",sizeof(long));
    printf("size of long long: %d bytes",sizeof(long long));
    return 0;
}